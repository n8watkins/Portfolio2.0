/**
 * Hero portrait rotation list. Drop additional images into public/hero/ and
 * add their paths here — the hero cycles through them automatically (3s each).
 * With a single entry the portrait is static.
 */
export const heroPortraits: string[] = ['/hero/portrait.jpg', '/hero/portrait-watercolor.jpg']
